# Pack transport manifest. Semantic authority remains in RDF, SPARQL, SHACL/ASK
# gates, and Tera. Consumers receive declared public outputs only and must not
# depend on repository-private implementation surfaces.
[pack]
name = "clap-noun-verb"

[pack.outputs]
queries = "queries"
templates = "templates"
